{"n": 1, "status": "open", "severity": "medium", "area": "signals", "file": "signals.ts, shared.ts, jsx.ts", "summary": "Two copies of railroad still can't work together: the load-time error says so, but the UI stays inert", "detail": "0.12's guard (signals.ts, 'One copy per page') logs an error naming both copies; it does not make them cooperate. The whole fix, only as one change: keep the tracking state (currentListener, currentDeps, batchDepth, activeFlush, pendingEffects, the dispose stack, the provider registry) on globalThis[Symbol.for('@blueshed/railroad@1')], and recognise a signal by that brand instead of `instanceof Signal`. A brand alone is worse than nothing: `{doc.data}` goes from a visible '[object Object]' to a silently stale value and when() still never switches, because .get() registers with its own copy's listener. Repro: link delta by `file:` so it brings its own node_modules/@blueshed/railroad, render `{doc.data}` and a when() on it. Until then the skill's 'Local development across repos' (pack tarballs, one copy) stands.", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 2, "status": "open", "severity": "medium", "area": "jsx, routes", "file": "jsx.ts, routes.ts", "summary": "A value change inside the same truthiness, or the same route pattern, doesn't re-render: when() and destructured params", "detail": "when(sig, fn) rebuilds only when truthiness flips, so /sites/42 → /sites/99 keeps showing site 42; a route handler that destructures ({ id }) runs once per pattern and keeps the first id. Both are documented now (skill §9, routes.ts header), not fixed. API options: when(cond, (v$) => …) passing a narrowed ReadonlySignal<NonNullable<T>> to the branch (also removes delta users' `!`); for routes, re-run a handler that doesn't take params$ (handler.length < 2) on a param change, or a `keyed` option like Solid's <Show keyed>. Breaking either way; decide before 1.0.", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 3, "status": "open", "severity": "low", "area": "signals", "file": "signals.ts", "summary": "Glitch-free only while computeds read the same signals every run: a computed that switches its reads can be seen half-updated", "detail": "Topological order is fixed at subscription depth; a computed like `flag.get() ? b.get() : a.get()` can deepen after its readers were ordered, so on a later write one reader may run once on half-updated values before it re-runs on the settled ones (every write still settles). The claim is narrowed in the signals.ts header, README, skill and CLAUDE.md. The real fix is push-dirty / pull-recompute (the colouring model of Preact signals, Vue 3.4, Reactively): a computed re-checks its sources when read. A randomized checker found 0 glitches on fixed graphs over ~107k runs and ~600 per 300 dynamic graphs; a 4-line partial ('notify readers when a computed deepens') left ~60 and added redundant runs, so it was not taken. Commit the checker with the fix.", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 4, "status": "open", "severity": "low", "area": "types", "file": "jsx.ts, index.ts, jsx-runtime.ts", "summary": "Importing the root barrel declares a global JSX namespace, which clashes with React's in a mixed app", "detail": "`import … from '@blueshed/railroad'` pulls jsx.ts's `declare global { namespace JSX }`, so a React app that imports the barrel gets TS2300 Duplicate identifier 'Element' (delta's client did, until its branch moved to subpaths). Subpath imports are clean. Fix (breaking for `jsx: react` users): move the global declaration to an opt-in `@blueshed/railroad/jsx-global` import; jsx-runtime.ts already covers `jsx: react-jsx`.", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 5, "status": "open", "severity": "low", "area": "jsx", "file": "jsx.ts, signals.ts", "summary": "A .get() in a render body is a silent one-shot read: warn once in development", "detail": "Now possible, since render bodies (components, when()/list() renders, route handlers) run untracked: warn once per signal when .get() runs with no listener inside a render. It would catch the skill's §1 (no .get() in JSX children), §7 and §9 traps at run time instead of in review.", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 6, "status": "open", "severity": "low", "area": "types", "file": "jsx.ts, jsx-runtime.ts", "summary": "IntrinsicElements accept anything: type them from HTMLElementTagNameMap", "detail": "Type each tag's props from HTMLElementTagNameMap / SVGElementTagNameMap with railroad's conventions (lowercase on* handlers as functions, `class`, `for`/htmlFor, Signal-or-function values), so a typo or a React-cased prop fails tsc. Check it against tests/consumer-types (no @types/bun).", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 7, "status": "open", "severity": "low", "area": "jsx", "file": "jsx.ts", "summary": "A function child that returns a Node renders it as text, with a warning", "detail": "`{() => cond.get() ? <A/> : <B/>}` warns and renders '[object HTMLDivElement]'. Either make a function child returning a Node a real dynamic region (bracket comments, dispose the old subtree, as when() does), or keep when()/list() as the only way and make the warning the documented answer. Decide with the when(cond, v$) item.", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 8, "status": "open", "severity": "low", "area": "routes", "file": "routes.ts", "summary": "RouteHandler still admits Promise<Node>, whose post-await bindings leak; navigate() sets the hash a tick late", "detail": "Kept for back-compat and documented: a bare Promise<Node> renders, but what it builds after its first await has no owner scope and outlives the route; the thunk form is the supported one. Deprecate then drop Promise<Node> from the type. navigate() is async by way of hashchange, so route() read straight after navigate() shows the old path; set the hash signal synchronously in navigate().", "note": "2026-09-24: from the round-2 review; open, for Peter."}
{"n": 9, "status": "open", "severity": "medium", "area": "toolset", "file": "(.claude/hooks/session-start.sh: railroad's and delta's; eta has none)", "summary": "No Bun-only SessionStart hook serves railroad, delta and eta: railroad's and delta's upgrade Bun with npm, and eta has none", "detail": "railroad's hook upgrades Bun with npm, runs `npx playwright install-deps`, decompresses Chromium with `node -e`, lets each of those print to stdout, and under `set -u` aborts when CLAUDE_ENV_FILE is unset; delta's logs to stderr and guards the env file, but installs Bun with `npm install -g bun` on every start, which moves the container's Bun to the latest release whatever the project pins, and it stages no browser; eta has no hook, so nothing provisions the Chromium its Bun.WebView tests need (eta's todo #18) and a fresh eta web session cannot go green without another repo's setup by hand. Each repo's CLAUDE.md says Bun only. round2/eta.md §6 Proposal 6, architect.md F8 and §6.4: one Bun-only hook shared by the three (bunx, bun -e; progress to stderr; a guarded env file) that detects what the project needs (the Bun floor from engines.bun, Chromium if a test file uses Bun.WebView, Postgres if pg is a dependency, with the DELTA_TEST_PG_URL convention delta documents) instead of hard-coding it. Same item in railroad's, delta's and eta's todo.jsonl.", "note": "2026-09-24: from the review team's round 1 and round 2; open, for Peter."}
{"n": 10, "status": "open", "severity": "medium", "area": "toolset", "file": "(no repo home; proposed as a marketplace plugin)", "summary": "No shared blueshed plugin or router skill tells an agent which of eta, delta and railroad to use, and delta-doc's own description captures every 'live multi-user app' request before railroad or eta are considered", "detail": "architect.md §5: delta-doc's SKILL.md claims every 'live multi-user app' request (its description names greenfield apps choosing between Firebase, Supabase and the rest, and the body says 'the answer is delta-doc') and never mentions eta or railroad by name as an alternative; eta-app only fires inside the eta repo, so a greenfield 'build me a live app' request never reaches it even though eta's own README pitches exactly that. Proposed shape (architect §6.2, proposal-final §5): one `blueshed` plugin in a marketplace repo holding a router skill under about 800 words (the decision guide: different people see different slices of the same data, undo matters, forms and lists, one box -> eta; interaction-heavy UI, several processes, Postgres/RLS, other clients consuming the doc JSON -> delta + railroad; one very interactive widget inside eta -> an applet, optionally a railroad island; reactive UI with no sync -> railroad alone), plus WIRE.md, one sandbox hook, one release command and MCP wiring; package skills stay inside the npm packages, synced at session start, restricted to @blueshed/* plus an allowlist (delta's install-skills does this since 02b1926), and delta-doc's own description narrows to projects that have already chosen delta. Namespacing under `blueshed:` ends skill/command shadowing in multi-repo sessions (architect F2, now largely moot since delta re-syncs its vendored railroad skills at each railroad release) and retires each repo's own /publish once `release` lands. Same item in railroad's, delta's and eta's todo.jsonl.", "note": "2026-09-24: from the review team's round 1; open, for Peter."}
{"n": 11, "status": "open", "severity": "low", "area": "toolset", "file": ".claude/commands/publish.md", "summary": "railroad, delta and eta each keep a copy of the one /publish text instead of one namespaced release command, and railroad's gate skips the browser suite its CI runs", "detail": "architect.md F8 found the three /publish commands had diverged: gates, changelog headings, commit and tag formats, Bun pinning in CI. One text now sits in all three (`.claude/commands/publish.md`, differing only in its table), with Keep a Changelog headings, `Release v<version>` commits and annotated tags. Left: three copies, which shadow each other in a session with more than one repo open and can drift again; railroad's gate is `bun run check && bun run check:consumer && bun test --coverage`, which skips the browser suite where no browser is found, while its CI runs `bun test ./tests/webview.test.ts` on its own step, so a release can pass /publish and fail CI; delta's gate is `bun run ci` (Docker, which its CLAUDE.md says fails in the sandbox: delta's todo #12). proposal-final §5: one `commands/release.md` under the blueshed plugin, namespaced so it stops shadowing, calling each repo's own sandbox-safe `bun run release:gate`. Same item in railroad's, delta's and eta's todo.jsonl.", "note": "2026-09-24: from the review team's round 1; open, for Peter. 2026-09-24 (round 2): one text across the three; what is left is above."}
{"n": 12, "status": "open", "severity": "low", "area": "toolset", "file": "package.json", "summary": "Nothing pins or CI-tests which railroad, delta and eta versions actually work together, so create-blueshed silently scaffolded delta 0.5.1 with an incompatible railroad 0.10.2", "detail": "architect.md F1 (Confirmed): a 0.x caret pins one minor, and bun printed no peer warning when the scaffold resolved a delta/railroad pair whose own peer range excluded it. delta's peer on railroad has since widened to `^0.11.0 || ^0.12.0`, which narrows the gap but nothing enforces it going forward, and eta now depends on both packages, so an untested pair can land in eta's own install, not only create-blueshed's. architect §6.3: a COMPAT.md in a marketplace repo, plus a create-blueshed CI job that scaffolds, installs latest releases and fails on an unmet peer (this would have caught F1), and delta's CI gaining a second job that runs its railroad-client suite (tests/railroad-client.test.ts) against `@blueshed/railroad@latest` and `main`. Same item in railroad's, delta's and eta's todo.jsonl.", "note": "2026-09-24: from the review team's round 1; open, for Peter."}
{"n": 13, "status": "open", "severity": "low", "area": "toolset", "file": "(none; proposed for a marketplace repo)", "summary": "No llms.txt exists for the toolset, so a non-Claude coding agent has no single discoverable entry point into eta, delta and railroad's skills and docs", "detail": "proposal-final §5/§6.2, Phase 3, optional: an llms.txt in the marketplace repo, linking to the router skill and to each package's skill at unpkg for a version, is S effort for non-Claude agents. Not the primary channel (a mega-skill or llms.txt alone would blow the reading budget and lose version truth, per architect §6.2), but worth doing alongside the plugin. Same item in railroad's, delta's and eta's todo.jsonl.", "note": "2026-09-24: from the review team's round 1; open, for Peter."}
{"n": 14, "status": "open", "severity": "low", "area": "docs", "file": ".claude/skills/bun-route/SKILL.md", "summary": "A two-tab / waitFor browser-test helper is hand-copied across bun-route's skill, eta's examples and a dogfooder's own tests instead of shipped once", "detail": "dogfood.md §7: \"the bun-route snippet, eta's app.test.ts waitFor, and my three copies are all the same function... The bun-route skill could ship it as a file.\" eta has since done exactly this on its own side (round2/eta.md §3: four copies of waitFor across chat, ticker, wedding and the kernel's client tests, 12-15 lines each, became one `src/webview.ts` with `at`, 32 lines), which is evidence the pattern is worth sharing rather than re-deriving per repo. bun-route's skill is the natural place to ship a canonical version that delta's and other examples' browser tests can import.", "note": "2026-09-24: from the review team's round 1; open, for Peter."}
